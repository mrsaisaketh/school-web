{
  "name": "school-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "comment": "Runtime dependencies live in the root package.json so that the Vercel /api function and this package resolve one identical copy of @prisma/client.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:seed": "node prisma/seed.js"
  }
}
